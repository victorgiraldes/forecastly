<%= content_for :title, "Forecastly" %>

<div class="search-container" data-controller="forecast-form">
  <h1>Get the forecast</h1>

  <form class="search-form" data-action="submit->forecast-form#submit">
    <label for="zip_code">ZIP Code</label>
    <input
      type="text"
      id="zip_code"
      name="zip_code"
      placeholder="e.g. 90210"
      maxlength="10"
      autocomplete="postal-code"
      data-forecast-form-target="input"
    >
    <button type="submit">Search</button>
  </form>

  <% if flash[:alert] %>
    <p class="alert"><%= flash[:alert] %></p>
  <% end %>
</div>
